Project: CityLink ASP.NET Core 9 MVC — C:\Users\Admin\Desktop\New folder\CityLink\CityLink-SCP\

## Task: Fix ServiceBookings and EventRegistrations admin tables

### Problem 1 — User info not displaying
Both section partials reference @b.User?.FirstName and @r.User?.FirstName but the
navigation properties come back null at runtime. The Index() action does have Includes:

  ServiceBookings = _context.ServiceBookings
      .Include(b => b.User).Include(b => b.Service)
      .OrderByDescending(b => b.Start_Time).ToList()

  EventRegistrations = _context.EventRegistrations
      .Include(r => r.User).Include(r => r.Event)
      .OrderByDescending(r => r.EventId).ToList()

Identity uses TPH (ApplicationUser base, ApplicationStaff derived, discriminator
column "UserType"). ServiceBooking.User and EventRegistration.User are both typed
as ApplicationUser. Diagnose why User is null and fix it — likely needs
.Include(b => b.User) to also eager-load through the TPH hierarchy, or the FK
config in CityLinksContext needs OnDelete reviewed.

Also add User email and phone columns to both tables so admins can contact the user
without needing to cross-reference the Users table.

### Problem 2 — No search on either table
These two tables have no search form and no server-side search endpoint.
The other five tables (Events, Services, Users, Staff, Feedback) already follow
this exact pattern — use it as the template:

  Pattern already in place:
  - Query param class in Common/QueryParameterClasses.cs (both exist already):
      ServiceBookingQueryParams: MinCost, MaxCost, From, To, UserId*, ServiceId*
      EventRegistrationQueryParams: MinAttendees, MaxAttendees, MinCost, MaxCost, UserId*, EventId*
      (* marked [UserRole("Staff")][QueryController("Admin")] — they DO bind correctly
         now that UserRole was fixed from "Admin" to "Staff")
  - ApplyQuery() extension in Common/QueryExtensions.cs (both exist already)
  - RestrictedQueryModelBinder handles TimeOnly and Enum types
  - Search endpoint in AdminController returns PartialView("_XxxTable", list)
  - _XxxTable.cshtml — table-only partial (what AJAX swaps)
  - _XxxSection.cshtml — search form (div.table-search > div.table-search__row)
    + <div id="xxx-table-container"><partial name="_XxxTable" /></div>
  - wireAdminSearch() in wwwroot/js/admin.js — form-driven, collects all
    named inputs/selects inside the form div, GETs /Admin/SearchXxx, swaps container

  CSS classes for the search form (already in admin.css):
  .table-search, .table-search__row, .table-search__field,
  .table-search__field--lg, .table-search__field--sm, .table-search__actions

  What to build:
  1. AdminController [HttpGet] SearchServiceBookings(ServiceBookingQueryParams query)
     - query.Size = 100
     - Include b.User and b.Service
     - return PartialView("_ServiceBookingsTable", list)

  2. AdminController [HttpGet] SearchEventRegistrations(EventRegistrationQueryParams query)
     - query.Size = 100
     - Include r.User and r.Event
     - return PartialView("_EventRegistrationsTable", list)

  3. Views/Admin/_ServiceBookingsTable.cshtml
     - Columns: Service title, User full name + email, Start, End, Total Cost, Delete btn
     - Delete button keeps existing data-userid + data-serviceid attributes
       (admin.js already handles .delete-booking-btn)

  4. Views/Admin/_EventRegistrationsTable.cshtml
     - Columns: Event title, User full name + email, Attendees, Total Cost, Delete btn
     - Delete button keeps existing data-userid + data-eventid attributes
       (admin.js already handles .delete-reg-btn)

  5. Rewrite Views/Admin/_ServiceBookingsSection.cshtml
     - Search form id="bookings-search-form" with one input per ServiceBookingQueryParams property:
         SearchTerm (if you add it, else MinCost/MaxCost/From/To are enough)
         MinCost (number), MaxCost (number)
         From (datetime-local), To (datetime-local)
         SortBy select: Default / Cost / Start Time
     - <div id="bookings-table-container"><partial name="_ServiceBookingsTable" /></div>

  6. Rewrite Views/Admin/_EventRegistrationsSection.cshtml
     - Search form id="registrations-search-form" with one input per EventRegistrationQueryParams:
         MinAttendees (number), MaxAttendees (number)
         MinCost (number), MaxCost (number)
         SortBy select: Default / Attendees / Cost
     - <div id="registrations-table-container"><partial name="_EventRegistrationsTable" /></div>

  7. Add two wireAdminSearch() calls at the bottom of the search wiring block in admin.js:
     wireAdminSearch({ formId: "bookings-search-form",       containerId: "bookings-table-container",       endpoint: "SearchServiceBookings",   searchBtnId: "bookingsSearchBtn",       clearBtnId: "bookingsClearBtn" });
     wireAdminSearch({ formId: "registrations-search-form",  containerId: "registrations-table-container",  endpoint: "SearchEventRegistrations", searchBtnId: "registrationsSearchBtn",  clearBtnId: "registrationsClearBtn" });

  The wireAdminSearch function already exists and handles everything — just pass the right IDs.
  Do a dotnet build to confirm 0 CS errors before finishing.